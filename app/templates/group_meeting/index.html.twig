{% extends 'base.html.twig' %}

{% block title %}Group meetings{% endblock %}

{% block body %}

<div class="app-page">
  <h1>Group Meetings</h1>

  <ol class="group-meetings app-list">

    {% for row in group_meetings_attendances %}
      <li>
        <a class="item" href="{{ path('group_meeting', {id: row.groupMeeting.id}) }}">
            <i class="fa fa-calendar" aria-hidden="true"></i>
            <div class="item-name">
              {{ row.groupMeeting.name  }}
            </div>
        </a>
      </li>
    {% endfor %}

  </ol>

{#    <details>#}
{#        {{ dump(group_meetings[0]) }}#}
{#    </details>#}


</div>

{% endblock %}
